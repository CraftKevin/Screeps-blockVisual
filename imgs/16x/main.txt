from PIL import Image
fn = 'road_WN-ES.png'
img = Image.open(fn)
w, h = (16, 16)
copy = img.copy()
img = Image.new('RGBA', (16, 16))
pix = img.load()
co_pix = copy.load()
for x in range(w):
    for y in range(h):
        pix[x, y] = co_pix[int(x * 4), int(y * 4)]
img.save(fn)